import { Router } from 'express';

const router = Router();

// Placeholder for trade routes
router.get('/', (req, res) => {
  res.json({ message: 'Trade routes not implemented yet' });
});

export default router;