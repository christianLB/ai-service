import { Router } from 'express';

const router = Router();

// Placeholder for strategy routes
router.get('/', (req, res) => {
  res.json({ message: 'Strategy routes not implemented yet' });
});

export default router;