import { Router } from 'express';

const router = Router();

// Placeholder for alert routes
router.get('/', (req, res) => {
  res.json({ message: 'Alert routes not implemented yet' });
});

export default router;